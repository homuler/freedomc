var fc = fc || {};
fc.thumbnail = fc.thumbnail || {};

(function(ns){
    ns.musicSrc = ns.musicSrc || {};
    ns.musicSrc["#{rawJS $ fromId2Text mid}"] =
        {
            path: "#{rawJS $ fCMusicDataSoundPath musicData}",
            format: "#{rawJS $ fCMusicDataFormat musicData}"
        };
    console.log(ns.musicSrc);
})(fc.thumbnail = fc.thumbnail || {});

window.onload = function(){
    fc.thumbnail.init();
};
